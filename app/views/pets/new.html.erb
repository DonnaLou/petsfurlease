<% provide(:title, "Create Pet Profile") %>

<div class="ui container">
	<div class="ui vertical stripe segment">
	  <h2 class="ui header">Create a Pet Profile</h2>
	  <%= render "petForm" %>
  </div>
</div>

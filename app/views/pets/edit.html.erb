<% provide(:title, "Edit Pet Profile") %>

<div class="ui container">
	<div class="ui vertical stripe segment">
	  <h2 class="ui teal header">Edit Pet Profile</h2>
	  <%= render "petForm" %>
  </div>
</div>
